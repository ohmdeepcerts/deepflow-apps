# Dependencies & build output (introduced in the architecture migration)
node_modules/
dist/
.vite/

# Environment / secrets
.env
.env.local
.env.*.local

# Claude Code local settings (machine-specific, not project source)
.claude/settings.local.json

# Manual backup snapshots — superseded by real git history from this point on
*.bak
*.bak-*

# Accidental artifacts
nul

# Playwright output
test-results/
playwright-report/
blob-report/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Ruflo local secrets and runtime data
.claude-flow/data/
.claude-flow/logs/
.claude-flow/sessions/
.claude-flow/daemon.pid
.claude-flow/daemon-state.json
.swarm/*.db
.swarm/*.index
.swarm/backups/
