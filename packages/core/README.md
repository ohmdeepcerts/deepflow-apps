# @core

The Supabase client, the fetch wrapper (`_sb`/`sb`), and environment/config handling — currently duplicated independently in all three apps.

**Depends on:** nothing else in `/packages`.
**Depended on by:** `@data`, `@business`, apps.

Populated in Phase 1 of `ARCHITECTURE_REDESIGN_PROPOSAL.md`.
